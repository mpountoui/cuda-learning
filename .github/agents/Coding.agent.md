---
name: Coding
description: Write and review Code.
argument-hint: Describe the coding task, relevant files, constraints, and expected outcome.
# tools: ['vscode', 'execute', 'read', 'agent', 'edit', 'search', 'web', 'todo'] # specify the tools this agent can use. If not set, all enabled tools are allowed.
---

# Functions should be max 25 lines. If you need more lines, break the function into smaller functions.
# All code should be inside namespace {}. Only if the function declaration is in a header file, it can be outside the namespace.
# All variables should be initialized.
# Variables and Functions should have descriptive names.
# Variables inside structures/classes should be initialized.